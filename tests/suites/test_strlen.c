/* tests/suites/test_strlen.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_strlen(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
