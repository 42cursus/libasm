/* tests/suites/test_strcpy.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_strcpy(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
