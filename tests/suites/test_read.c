/* tests/suites/test_read.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_read(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
