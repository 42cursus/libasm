/* tests/suites/test_strcmp.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_strcmp(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
