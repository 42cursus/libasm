/* tests/suites/test_write.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_write(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
