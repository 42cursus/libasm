/* tests/suites/test_strdup.c — TODO: real assertions in the next commit. */
#include "../test.h"
#include "suites.h"

void	test_strdup(void)
{
/* placeholder so the dispatcher links; real assertions follow. */
}
